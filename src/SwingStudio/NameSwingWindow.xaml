<Window x:Class="SwingStudio.NameSwingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Swing"
        Width="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Assets/SwingStudio.ico"
        Background="#1E1E1E"
        Loaded="Window_Loaded">
    <StackPanel Margin="20">
        <TextBlock Text="Name" Foreground="#C8C8C8" Margin="0,0,0,6" />
        <TextBox x:Name="SwingNameBox" AutomationProperties.Name="Swing name" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="88" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="OK" Width="88" IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
