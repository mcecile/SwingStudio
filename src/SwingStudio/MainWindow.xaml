<Window x:Class="SwingStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SwingStudio"
        Height="800"
        Width="1280"
        MinHeight="480"
        MinWidth="800"
        Background="#1E1E1E">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="140" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4">
                <TextBlock Text="Camera A" Foreground="#9A9A9A" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
            </Border>
            <Border Grid.Column="2" Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4">
                <TextBlock Text="Camera B" Foreground="#9A9A9A" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
            </Border>
        </Grid>

        <Border Grid.Row="1" Margin="0,12,0,12" Padding="12,8" Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4">
            <TextBlock Text="Live" Foreground="#C8C8C8" VerticalAlignment="Center" />
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Microphone" Foreground="#C8C8C8" Margin="0,0,0,8" />
                    <ProgressBar Height="8" Value="0" Maximum="100" VerticalAlignment="Center" />
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4">
                <TextBlock Text="Pressure" Foreground="#9A9A9A" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
            </Border>
        </Grid>

        <TextBlock x:Name="SessionsPathText" Grid.Row="3" Margin="0,8,0,0" Foreground="#7A7A7A" TextWrapping="Wrap" />
    </Grid>
</Window>
