<Window x:Class="SwingStudio.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Assets/SwingStudio.ico"
        Background="#1E1E1E">
    <Window.Resources>
        <Style x:Key="SpinButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="#3A3A3A" />
            <Setter Property="Foreground" Value="#E6E6E6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="8" />
            <Setter Property="Height" Value="11" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Delay" Value="400" />
            <Setter Property="Interval" Value="60" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#505050" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#606060" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <StackPanel Margin="20">
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="28" />
                <ColumnDefinition Width="160" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Trigger threshold" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="36" />
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="ThresholdSlider" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True"
                            VerticalAlignment="Center" AutomationProperties.Name="Trigger threshold"
                            ValueChanged="ThresholdSlider_ValueChanged" />
                    <TextBlock x:Name="ThresholdValue" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#E6E6E6" />
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button x:Name="CalibrateButton" Content="Calibrate..." Padding="12,0" Click="Calibrate_Click" AutomationProperties.Name="Calibrate trigger" />
                    <TextBlock x:Name="CalibrateHint" Foreground="#7A7A7A" VerticalAlignment="Center" Margin="10,0,0,0" TextWrapping="Wrap" />
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Trigger offset (ms)" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <TextBox x:Name="TriggerOffset" AutomationProperties.Name="Trigger offset" />
            </StackPanel>
        </Grid>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="28" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Seconds before impact" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <DockPanel HorizontalAlignment="Left" Width="140">
                    <StackPanel DockPanel.Dock="Right" Width="18" Margin="2,0,0,0">
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70E;" Margin="0,0,0,1" Click="SecondsBeforeUp_Click" AutomationProperties.Name="Increase seconds before impact" />
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70D;" Click="SecondsBeforeDown_Click" AutomationProperties.Name="Decrease seconds before impact" />
                    </StackPanel>
                    <TextBox x:Name="SecondsBefore" VerticalAlignment="Center" AutomationProperties.Name="Seconds before impact" />
                </DockPanel>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Seconds after impact" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <DockPanel HorizontalAlignment="Left" Width="140">
                    <StackPanel DockPanel.Dock="Right" Width="18" Margin="2,0,0,0">
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70E;" Margin="0,0,0,1" Click="SecondsAfterUp_Click" AutomationProperties.Name="Increase seconds after impact" />
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70D;" Click="SecondsAfterDown_Click" AutomationProperties.Name="Decrease seconds after impact" />
                    </StackPanel>
                    <TextBox x:Name="SecondsAfter" VerticalAlignment="Center" AutomationProperties.Name="Seconds after impact" />
                </DockPanel>
            </StackPanel>
        </Grid>

        <TextBlock Text="Capture mode" Foreground="#C8C8C8" Margin="0,0,0,6" />
        <ComboBox x:Name="CaptureModeList" Margin="0,0,0,6" DisplayMemberPath="Label" AutomationProperties.Name="Capture mode" />
        <TextBlock x:Name="CaptureModeHint" Foreground="#7A7A7A" TextWrapping="Wrap" Margin="0,0,0,16" />

        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Session folder" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Browse" Width="88" Margin="8,0,0,0" Click="Browse_Click" AutomationProperties.Name="Browse session folder" />
                    <TextBox x:Name="SessionFolder" VerticalAlignment="Center" AutomationProperties.Name="Session folder" />
                </DockPanel>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Swings to keep" Foreground="#C8C8C8" Margin="0,0,0,6" />
                <DockPanel>
                    <StackPanel DockPanel.Dock="Right" Width="18" Margin="2,0,0,0">
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70E;" Margin="0,0,0,1" Click="SwingsToKeepUp_Click" AutomationProperties.Name="Increase swings to keep" />
                        <RepeatButton Style="{StaticResource SpinButton}" Content="&#xE70D;" Click="SwingsToKeepDown_Click" AutomationProperties.Name="Decrease swings to keep" />
                    </StackPanel>
                    <TextBox x:Name="SwingsToKeep" VerticalAlignment="Center" AutomationProperties.Name="Swings to keep" />
                </DockPanel>
            </StackPanel>
        </Grid>

        <DockPanel>
            <Button DockPanel.Dock="Left" Content="Open log folder" Padding="12,0" Click="OpenLogFolder_Click" AutomationProperties.Name="Open log folder" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="88" Margin="0,0,8,0" IsCancel="True" />
                <Button Content="OK" Width="88" IsDefault="True" Click="Ok_Click" />
            </StackPanel>
        </DockPanel>
    </StackPanel>
</Window>
